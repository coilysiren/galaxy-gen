name: ci

on:
  pull_request:

jobs:
  gate:
    runs-on: docker
    container: forgejo.coilysiren.me/coilyco-flight-deck/agentic-os:release
    steps:
      - uses: actions/checkout@v6
      - run: just ci-setup
      - run: just lint-rust
      - run: just test-rust
      - run: just check-js
