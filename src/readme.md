`src/*` contains the rust logic, wrapped in calls to [wasm-bindgen](https://github.com/rustwasm/wasm-bindgen) to provide a js api

the files here are compiled to `./../client/assets/built-wasm/` as `wasm` + `js`
