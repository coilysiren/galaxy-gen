# Consumer-side opt-outs for agentic-os tree-walking hooks.
# The v0.6.0 hook bump (required to drop providesApis, see #14 / parent #433)
# tightened documentation-layout to cap every Markdown file at 80 lines /
# 4000 chars and enforce flat skill folders + a root-Markdown allowlist. The
# entries below are pre-existing doc debt surfaced by that tightening, not by
# the catalog migration. They are excluded to unblock the bump; the real
# cleanup (rehome the journal and the oversized skills, restructure
# AGENTS.md for catalog-trifecta) is tracked as a follow-up.

[documentation-layout]
excludes = [
  "AGENTS.md",
  "README.md",
  "docs/FEATURES.md",
  "docs/journal/",
  "pkg/",
  ".agents/skills/coding-galaxy-gen-astrophysics/",
  ".agents/skills/coding-galaxy-gen-cosmology/",
  ".agents/skills/coding-galaxy-gen-references/",
  ".agents/skills/tooling-skill-authoring/",
]

[catalog-trifecta]
# AGENTS.md predates the 8-section trifecta layout; restructuring it is part
# of the doc-cleanup follow-up, not this catalog migration.
enabled = false

[code-comments]
# The v0.21.0+ code-comments hook allows YAML comments only in the top header
# block (a key-sorter would drift a mid-file comment from its target). This
# repo's YAML is operational config - a multi-document k8s manifest, CI
# workflows, command allowlists - whose inline comments explain the specific
# resource or key they sit beside, so hoisting them to a top header or docs/
# would strip that locality. Excluded here (the same escape hatch eco-* repos
# use to skip code-comments) so the hook still governs Rust/TS/JS source, where
# the two-line rule fits. See galaxy-gen#24.
excludes = [
  "deploy/",
  ".forgejo/",
  ".github/",
  ".ward/",
  ".agents/skills/categories.yaml",
  ".pre-commit-config.yaml",
]
